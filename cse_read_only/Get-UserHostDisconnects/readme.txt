Get-UserHostDisconnects

This script will retrieve all host disconnects that have been manually initiated by a User.

To run the script, you will need to run it with powercli.

It will prompt you for a vCenter, and then do the rest on it's own. 

Results will be displayed to the screen using Out-Gridview