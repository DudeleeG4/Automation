Get-EverythingOnDS

This script is as simple as it's name suggests, it simply lists every file on a datastore along with it's size and last modified date.

To run the script, run it from any jumpbox with PowerCLI installed and access to the vCenter you want to retrieve Datastore files from.