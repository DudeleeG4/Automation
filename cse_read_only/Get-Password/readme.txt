Get-Password.ps1

This is a ver simply script to generate a complex password of variable length. It will always contain at least one uppercase, one lowercase, one digit and one special character.


To run the script, simply right-click and select "Run with powershell", you will then be prompted to provide a number. This number is the length of the password.
