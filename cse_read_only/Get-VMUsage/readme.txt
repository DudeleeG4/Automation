vmusage.ps1

This isn't really a script it's just something that I need to turn into a function. It just retrieves the CPU and Memory reservation and current utilisation stats for a VM.

This script isn't "Run", it's just used as part of other scripts.