Get-VMDeltas

The purpose of this script is to allow someone to identify long snapshot chains on VMs because it looks for snapshots with numbers higher than 6 at the end.

To run the script, right-click it and select "Run with powershell". Currently it is hardcoded for IL3 vCenters