Get-FolderSizes.ps1

This is another "script" that is actually just waiting to be turned into a function. It simply lists the total size of a folder in Windows by diving in recursively and reporting the summation of all the files' individual sizes.