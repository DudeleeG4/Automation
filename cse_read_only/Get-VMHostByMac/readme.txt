Get-HostByMac

The purpose of this script is to put in a MAC address and the script will return the host associated with it.

In order to run the script, open powercli and navigate to the directory with the script in it, then run the script from there.

It will prompt you to enter the vCenter, and then the MAC address.

Results will be displayed in the pipeline.