This is a simple script to search for a VM by it's mac address. It can be handy to find VMs with duplicate mac addresses.

To run the script, open powercli and run it from there. It will prompt you to enter the vCenter and the MAC address you are searching for.

Results will output to the console