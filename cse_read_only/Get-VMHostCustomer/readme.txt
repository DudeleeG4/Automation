Get-VMHostCustomer

This is a script which will pull off a list of every VM for a given vCenter, and will list what host it is on and what the customer is for the VM.

This is useful for when we need to complete rolling reboots of Pod B / F and need to communicate with Docman.

